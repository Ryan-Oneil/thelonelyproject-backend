server:
  port: 80
  tempDirectory: F:\media\test

logging.level.org.springframework.web: DEBUG

spring:
  servlet:
    multipart:
      enabled: false
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/lonelyproject?useSSL=false&amp
    username: lonelyproject
    password: ${DB_PASS}
  ssl: false
  liquibase:
    default-schema: public

cdn:
  url: https://cdn.lonelyproject.org/file

backblaze:
  url: https://api001.backblazeb2.com
  buckets:
    avatars:
      id: 4b83175351b6fd4178e50519
      name: lp-avatars

